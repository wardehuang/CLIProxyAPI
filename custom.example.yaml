# Optional local-only overrides loaded from custom.yaml next to config.yaml.
# Claude Code compact requests can use lighter replacement models while preserving other compact handling.
codex-compact-model: gpt-5.4-mini
antigravity-compact-model: gemini-3.1-flash-lite
